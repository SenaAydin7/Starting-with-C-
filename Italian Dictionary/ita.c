#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
            printf("Imparare Italiano");
            printf("\n\n\n\n\n");
            
            int essen=mangiare;
            if(essen==mangiare);
            
            {
            printf("Richtig");	
	            }	return 0;
}
